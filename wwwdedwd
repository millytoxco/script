getgenv().Private = {
    Main = {
        Build = "V1"-- dont change
    },
    SilentAim = {
        Enabled = true,
        SilentHitChance = 100,
        TargetHitPart = "Head",
        SafeMode = true,
        DynamicPrediction = false,
        PredictionForSilent = 0.01,
        LockedTarget = nil,
        IsBeingSpectated = nil,
        OriginalIndex = nil,
        AntiAimViewer = true,
        FOV = 290, 
        ShowFOV = false, 
        FOVColor = Color3.fromRGB(255, 255, 255), 
        FOVThickness = 1.5, 
        FOVTransparency = 0.7 
    },
    Aimbot = {
        Enabled = true,
        Smoothness = 0.25,
        AimPart = "Head",
        FOV = 150,
        LockOnKey = Enum.KeyCode.Q,
        LockedTarget = nil
    },
    ESP = {
        Enabled = false,         -- Master ESP toggle
        Color = Color3.fromRGB(0,0,0), -- ESP color (black)
        ShowNames = true,       -- Show player names
        ShowTracers = true,     -- Show tracers
        ShowDistance = true,    -- Show distance
        ShowHealth = false      -- Show health (set to false, no health check)
    }
}
loadstring(game:HttpGet("https://api.luarmor.net/files/v3/loaders/bcff791183d153e015aa91184fa191ec.lua"))()
